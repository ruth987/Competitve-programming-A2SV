class Solution:
    def arraySortedOrNot(self, arr, n):
        # code here
        sorted_ = True
        for i in range(1, n):
            if arr[i] < arr[i-1]:
                sorted_ = False
        return sorted_
